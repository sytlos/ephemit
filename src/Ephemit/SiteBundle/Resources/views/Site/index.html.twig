{% extends '::base.html.twig' %}
{% block body %}

<a href="{{path('ephemit_evenement_creer')}}">Cr&eacute;er une page</a><br>
{% if is_granted("IS_AUTHENTICATED_FULLY") %}
    <p>Bienvenue, {{ app.user.username }}</p>
    <a href="{{path('ephemit_evenement_list')}}">Mes &eacute;v&egrave;nements</a><br>
    <a href="{{path('fos_user_security_logout')}}">Disconnect</a>
    
{% else %}
    <a href="{{path('fos_user_registration_register')}}">Register</a><br>
    <a href="{{path('fos_user_security_login')}}">Connect</a><br>
{% endif %}
    
{% render controller('EphemitSiteBundle:Site:searchPage') %}

    

{% endblock %}